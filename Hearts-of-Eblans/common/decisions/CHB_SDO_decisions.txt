CHB_SDO_management_cat = {

	# --- ACTIVE EFFORTS ---

	# Counter-propaganda: Stops friction for 30 days
	CHB_SDO_counter_propaganda = {
		icon = generic_propaganda
		cost = 25
		available = {
			has_political_power > 25
		}
		visible = {
			has_war_with = DRE
		}
		complete_effect = {
			add_timed_idea = { idea = CHB_SDO_counter_propaganda_spirit days = 30 }
			add_to_variable = { global.SDO_peace_progress = 0.02 }
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				has_idea = CHB_SDO_counter_propaganda_spirit
			}
		}
	}

	# Humanitarian Outreach: + Progress, but costs Stability
	CHB_SDO_humanitarian_initiative = {
		icon = generic_volunteer_expedition
		cost = 50
		available = {
			has_political_power > 50
			has_stability > 0.2
		}
		visible = {
			has_war_with = DRE
		}
		complete_effect = {
			add_to_variable = { global.SDO_peace_progress = 0.08 }
			add_stability = -0.02
		}
		ai_will_do = { factor = 1 }
	}

	# Secret Channels: Slow progress over time
	CHB_SDO_secret_negotiations = {
		icon = generic_diplomacy
		cost = 75
		days_remove = 60
		available = {
			has_political_power > 75
		}
		visible = {
			has_war_with = DRE
		}
		modifier = {
			political_power_gain = -0.15
		}
		remove_effect = {
			add_to_variable = { global.SDO_peace_progress = 0.12 }
		}
		ai_will_do = { factor = 1 }
	}

	# --- PEACE OPTIONS ---

	# Peace at Frontline Proposal (50% progress required)
	CHB_SDO_propose_frontline_peace = {
		icon = generic_closure
		cost = 100
		available = {
			custom_trigger_tooltip = {
				tooltip = CHB_SDO_progress_50_tooltip
				check_variable = { var = global.SDO_peace_progress value > 0.499 }
			}
			has_political_power > 100
		}
		visible = {
			has_war_with = DRE
		}
		complete_effect = {
			DRE = { country_event = { id = SDO_event.1 } }
		}
		ai_will_do = { factor = 0 }
	}

	# White Peace Proposal (75% progress required)
	CHB_SDO_propose_white_peace = {
		icon = generic_reparation
		cost = 150
		available = {
			custom_trigger_tooltip = {
				tooltip = CHB_SDO_progress_75_tooltip
				check_variable = { var = global.SDO_peace_progress value > 0.74 }
			}
			has_political_power > 150
		}
		visible = {
			has_war_with = DRE
		}
		complete_effect = {
			DRE = { country_event = { id = SDO_event.2 } }
		}
		ai_will_do = { factor = 0 }
	}

	# Vitaly's Treaty (100% progress required)
	CHB_SDO_vitaly_treaty = {
		icon = generic_victory
		cost = 0
		available = {
			custom_trigger_tooltip = {
				tooltip = CHB_SDO_progress_100_tooltip
				check_variable = { var = global.SDO_peace_progress value > 0.99 }
			}
		}
		visible = {
			has_war_with = DRE
		}
		complete_effect = {
			DRE = { country_event = { id = SDO_event.3 } }
		}
		ai_will_do = { factor = 0 }
	}
}
